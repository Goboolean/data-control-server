services:
  server:
    container_name: stock-fetch-server
    build:
      context: ../
      dockerfile: ./build/Dockerfile
    ports:
      - "7001:8080"
    command: ["go", "run", "cmd/main/run.go"]